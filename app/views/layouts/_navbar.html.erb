<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
	<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>

        <% if user_signed_in? %>
          <a class="navbar-brand" href="/movies">Filmy animowane</a>
        <% else %>
          <a class="navbar-brand" href="/">Filmy animowane</a>
        <% end %>


        <div id="navbar" class="navbar-text pull-right">
           <% if user_signed_in? %>
             Zalogowano jako <strong><%= current_user.email %></strong>.
             <%= link_to 'Edytuj profil', edit_user_registration_path, :class => 'navbar-link' %> |
             <%= link_to "Wyloguj", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
           <% else %>
             <%= link_to "Rejestracja", new_user_registration_path, :class => 'navbar-link'  %> |
             <%= link_to "Logowanie", new_user_session_path, :class => 'navbar-link'  %>
           <% end %>
        </div>
      </div>
    </nav>
  </div>
</header>
